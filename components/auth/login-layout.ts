import type { LoginHeroLayout } from "./LoginHero";

/**
 * Ajuste centralizado da coluna hero da tela de login.
 * Edite os campos desejados para movimentar o conteúdo sem tocar nos componentes.
 *
 * Exemplos:
 *  - offsetY: -48 (sobe) ou 32 (desce) pixels
 *  - align: "top" | "center" | "bottom"
 *  - padding: "px-10 py-12 lg:px-20 lg:py-16"
 */
export const loginHeroLayoutOverrides: Partial<LoginHeroLayout> = {
  offsetY: -0,
};
